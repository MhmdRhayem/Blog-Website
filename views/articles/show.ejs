<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Article</title>
</head>
<body>
    <%if(locals.article){%>
        <div class="container">
            <div class=" mt-4">
                <div class="card-body">
                    <h1 class="card-title"><%= locals.article.title %></h1>
                    <div class="card-subtitle text-muted mb-2"><%= article.date_created %></div>
                    <a href="/" class="btn btn-secondary mb-2">All Articles</a>
                    <a class="btn btn-info mb-2" href = "/articles/edit/<%= article.id %>">Edit</a>
                    <hr>
                    <p class="card-text"><%= locals.article.description %></p>
        </div>
    <%}%>
</body>
</html>